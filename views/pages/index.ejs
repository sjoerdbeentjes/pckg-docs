<%- include('../partials/head'); -%>

<body>
  <nav class="sidebar">
    <ul>
      <% packages.forEach(package => { %>
        <li>
          <a class="<%= package === activePackage ? 'active' : '' %>" href="/<%= package %>">
            <span class="package-name"><%= package %></span>
          </a>
        </li>
      <% }) %>
    </ul>
  </nav>

  <main class="body markdown-body">
    <%- readmeHtml ? readmeHtml : '' %>
  </main>
</body>

